{
  "schema_version": "scc.skill.v1",
  "skill_id": "stability.control",
  "version": "1.0.0",
  "owner_role": "stability_controller",
  "summary": "Apply backpressure/circuit-breaking decisions (limit concurrency, isolate lanes, degrade verification).",
  "applies_to": { "task_class": ["*"] },
  "contracts": {
    "input_schema": "contracts/factory_policy/factory_policy.schema.json",
    "output_schema": "contracts/envelope/envelope.schema.json"
  },
  "budgets": { "max_actions": 20 },
  "quality_gates": { "must_be_idempotent": true },
  "enablement": { "status": "active", "rollout": { "mode": "all", "percent": 100 } }
}

