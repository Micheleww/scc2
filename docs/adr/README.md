# ADR (Architecture Decision Records)

命名：`docs/adr/ADR-YYYYMMDD-<slug>.md`

6 行模板（必须逐行保留前缀）：
1. Context:
2. Decision:
3. Alternatives:
4. Consequences:
5. Migration:
6. Owner:

门禁：
- 新增依赖/目录/协议变更 → 必须新增或更新 ADR。
- ADR 必须被 SSOT 登记（`docs/INDEX.md`）。

